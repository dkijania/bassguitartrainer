<UserControl x:Class="BassNotesMaster.WpfControls.FretboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="170" Width="970">
    <Grid Name="Container" Background="Transparent" >
        <Canvas x:Name="MainDrawingArena">
            <Image x:Name="FretBoard" HorizontalAlignment="Left" VerticalAlignment="Top" Height="170" Width="972" />
        </Canvas>
        <Canvas x:Name="selectionCanvas">
            <Border x:Name="selectionBox" BorderBrush="Blue" BorderThickness="1" Background="LightBlue"
                        CornerRadius="1" Opacity="0.5" Visibility="Collapsed" />
        </Canvas>
    </Grid>
</UserControl>
